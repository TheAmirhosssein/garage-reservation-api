exclude: .*migrations\/.*
repos:
  - repo: https://github.com/charliermarsh/ruff-pre-commit
    rev: v0.12.11  # Use the latest stable version
    hooks:
      - id: ruff
        args: ["--fix"]  # Automatically fix issues where possible
        additional_dependencies: []
        files: \.py$
